include ./table-data

.section.section--table.section--g-table
  .breadcrumbs__wrap
    .breadcrumbs__item
      a.breadcrumbs__link(href='')
        | Home

    .breadcrumbs__item
      a.breadcrumbs__link(href='')
        | Reports

    .breadcrumbs__item
      a.breadcrumbs__link(href='')
        | Top States Where Cryptocurrency Is Catching On

    .breadcrumbs__item
      | Full Ranking

  h2.section__title.section__title--table
    | Full Ranking

  .ranking-table__container
    .ranking-table__hint
      .ranking-table__hint-close-btn
      p.ranking-table__hint-text
        | By default cities are sorted
        br
        | by alphabet. 
        span.semibold
          | You could sort by other parameter.

    .mobile-only
      table.ranking-table.ranking-table--fixed
        thead
          tr
            th State

        tbody
          each value in values
            tr
              td
                = value.state

    .ranking-table__wrapper
      table.ranking-table.ranking-table--data
        thead
          tr
            th(class='table-sort' data-index='0') State
            th(class='table-sort' data-index='1') Infrastructure Rank
            th(class='table-sort' data-index='2') Interest in Cryptocurrency
            th(class='table-sort' data-index='3')
              | Interest In Cryptocurrencies
              span.reg *
            th(class='table-sort' data-index='4') Bitcoin ATMs
            th(class='table-sort' data-index='5') Bitcoin Establishments
            th(class='table-sort' data-index='6') Average Interest

        tbody
          each value in values
            tr
              td
                = value.state

              td
                = value.infrastructureRank

              td
                = value.cryptocurrency

              td
                = value.cryptocurrencies

              td
                = value.atms

              td
                = value.establishments

              td
                = value.averageInterest

  .btn-container.btn-container--under-table
    a.btn-go-back(href="index.html")
      | GO BACK TO THE FULL REPORT
